// eli15/chapters/genetic-algorithm.js
// Selection + mutation + novelty — how the population gets better without gradients.
export default {
  id: 'genetic-algorithm',
  title: 'Breeding brains instead of training them',
  oneLiner: 'Copy the winners, nudge their weights, discard the losers. Repeat.',
  body: [
    '<p>Normal neural networks learn with <strong>backpropagation</strong>: compute a',
    'gradient, adjust every weight a tiny bit in the direction that reduces the loss.',
    'We do none of that. Instead we run a <strong>genetic algorithm</strong> — the brute-force,',
    'gradient-free cousin of learning. Open <code>main.js:85-97</code> and you\'ll see the',
    'whole breeding loop:</p>',
    '<ol>',
    '<li><strong>Elite (1 car)</strong>: copy the top brain from the archive verbatim.',
    'No mutation. This is our safety net — at worst the next batch ties the best batch.</li>',
    '<li><strong>Light mutation (~half of the rest)</strong>: copy a good brain, then wiggle',
    'every weight by <code>mutateValue * 0.5</code>. Small perturbations — usually a small',
    'improvement or a small loss.</li>',
    '<li><strong>Heavy mutation (~half of the rest)</strong>: copy a good brain, wiggle by',
    '<code>min(1, mutateValue * 1.8)</code>. Much more exploratory — most die, but the',
    'occasional lucky mutation jumps to a new local optimum.</li>',
    '<li><strong>Novelty (≥1 car)</strong>: don\'t copy anything. Brand-new random brain.',
    'Prevents the whole population from collapsing into minor variations of one idea.</li>',
    '</ol>',
    '<p>At the end of every batch, <code>nextBatch()</code> picks the single best-performing',
    'car (by <code>fitness-function</code>) and overwrites <code>localStorage.bestBrain</code>.',
    'Next generation is seeded from that. No gradients, no derivatives, no math beyond',
    '<code>lerp</code>. That\'s it. Over hundreds of generations the wiggling converges on',
    'a brain that can actually drive the track.</p>',
    '<h3>Baseline vs. enhanced</h3>',
    '<p>This genetic algorithm is the <strong>baseline</strong> — everything else you see on',
    'the right-hand panel (vector memory, reranker, LoRA adapter, SONA trajectories, lineage',
    'DAG) is a layer of <em>enhancement</em> stacked on top. The baseline alone is sufficient',
    'to learn to drive. The enhancements just make learning <em>faster</em> by reusing brains',
    'from similar tracks, nudging track embeddings toward successful neighbours, and',
    'clustering trajectory patterns across generations.</p>',
    '<p>You can see this for yourself with the <strong>Vector Memory</strong> toggle at the',
    'top of the right-hand panel. Flip it <strong>off</strong> and every <code>bridgeReady()</code>',
    'check in the code returns false — no retrieval, no seeding from archive, no reranker,',
    'no adapter, no pattern extraction. Cars still drive, still mutate, still get better',
    'over generations. The Vector Memory panel goes cold. Flip it <strong>on</strong> and',
    'the seed list repopulates, the lineage DAG starts wiring itself up, and warm-starting',
    'on a similar track kicks in again.</p>',
    '<p>If the bridge genuinely fails to load (e.g. the dev server was launched from the',
    'wrong directory and <code>../vendor/…</code> can\'t resolve), you get the same',
    'experience as flipping the toggle off — a red banner explains what went wrong, but',
    'training continues on the baseline path.</p>',
    '<h3>Try it yourself</h3>',
    '<ul>',
    '<li>In phase 4, drag the <strong>Variance</strong> slider up toward 0.3 and watch the',
    'cars get chaotic — lots of spinouts but occasional breakthroughs. Drag it near 0 and',
    'training plateaus: no exploration, no progress.</li>',
    '<li>Train for a few generations with <strong>Vector Memory</strong> off, note the',
    'Fast Lap. Flip it on, reset the brain, train the same number of generations again.',
    'On a track that resembles one you\'ve trained before, the enhanced path converges',
    'noticeably faster because begin() seeds the population from archived winners.</li>',
    '</ul>',
  ].join('\n'),
  diagram: [
    '<pre class="eli15-ascii">',
    '                 generation N',
    '   ┌────────────────────────────────────┐',
    '   │  1× elite (best brain, unchanged)  │',
    '   │  ~N/2 light mutants (amount=0.15)  │',
    '   │  ~N/2 heavy mutants (amount=0.54)  │',
    '   │  1× novelty (fresh random brain)   │',
    '   └────────────────────────────────────┘',
    '                    │',
    '                    ▼',
    '          race all cars on track',
    '                    │',
    '                    ▼',
    '   pick the best car  →  becomes next gen\'s elite',
    '</pre>',
  ].join('\n'),
  related: [
    'fitness-function',
    'neural-network',
    'lineage',
  ],
};
