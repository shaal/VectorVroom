// eli15/chapters/cross-tab-federation.js
// Phase 2B (F6) — cross-tab live training. Ships with the F6 wiring.
export default {
  id: 'cross-tab-federation',
  title: 'Two browser tabs training in sync',
  oneLiner: 'Open two tabs with ?crosstab=1; a brain archived in one appears in the other in real time, with no server.',
  body: [
    '<p><strong>The one-line version.</strong> When you open the same page twice,',
    'both tabs share the archive through a browser primitive called',
    '<code>BroadcastChannel</code>. Each time a brain is added in tab A, a tiny',
    'message flies to tab B (and any other tabs); tab B inserts it into its own',
    'archive. No server. No sockets. Just the browser itself.</p>',

    '<h3>What BroadcastChannel is (and isn\'t)</h3>',
    '<p><code>new BroadcastChannel(\'vectorvroom-archive\')</code> gives every',
    'same-origin tab a shared bus. <code>postMessage</code> on one end shows up',
    'as a <code>message</code> event on every other end — but <em>not</em> on the',
    'sender. It\'s synchronous-ish (microtask-delivered), has no network hop, and',
    'structured-clones the payload so you can send typed arrays without',
    'stringifying. It\'s also ephemeral: there\'s no history, no replay. If a tab',
    'is closed when a message is sent, it missed it — forever.</p>',

    '<h3>Why this is lockless by construction</h3>',
    '<p>Two tabs broadcasting at each other sounds like a recipe for conflicts:',
    'what if both tabs independently archive the <em>same</em> brain at nearly',
    'the same instant? In a classical system you\'d need a lock, or a CRDT, or a',
    'server to pick a winner.</p>',
    '<p>We don\'t need any of that, because Phase 1D made brains',
    '<em>content-addressed</em>. The id of a brain is the hash of its weights',
    '(<code>xxHash32</code> over the flattened <code>Float32Array</code>). Two',
    'tabs that compute the same brain compute the same hash — deterministically,',
    'byte-for-byte — and the dedup table in <code>archive/dedup.js</code>',
    'collapses any repeat into the canonical node. So the protocol is simply:',
    '<em>archive locally, broadcast eagerly, trust the hash</em>. If the same',
    'brain arrives twice, the second insert is a no-op. If it never arrives, the',
    'local archive is still correct. There is no coordinator and nothing to',
    'agree on.</p>',

    '<h3>Preventing echo loops</h3>',
    '<p>The naive wiring loops forever: tab A archives → broadcasts → tab B',
    'receives → archives → broadcasts → tab A receives → archives → broadcasts…',
    'BroadcastChannel doesn\'t echo to the <em>sender</em>, which stops the',
    'trivial case, but tab A\'s broadcast still returns to A through tab B\'s',
    're-broadcast. Two guards stop this. First, every message carries a random',
    'per-tab <code>senderId</code>; we drop any message whose senderId equals',
    'our own (defense-in-depth for future relay setups). Second, and more',
    'importantly, when the bridge receives a remote brain it sets a',
    '<code>_crosstabReceiving</code> flag before calling <code>archiveBrain</code>',
    '— and the broadcast hook checks that flag and declines to re-emit. So the',
    'receive path is a pure one-way archive update, never a re-broadcast.</p>',

    '<h3>A worked example</h3>',
    '<p>Imagine tab A trains on a rectangular track and stumbles onto a brain',
    'that can lap cleanly (fitness 950). <code>archiveBrain</code> hashes the',
    'weights to <code>7f3a1c08</code>, inserts it locally, and posts',
    '<code>{type: \'brain\', senderId: \'a1b2\', payload: {flat, hash: \'7f3a1c08\',',
    'fitness: 950, …}}</code> on the channel. A millisecond later tab B\'s',
    '<code>message</code> handler fires; it decodes the payload, calls its own',
    '<code>archiveBrain</code> under the receive guard. The hash hits the dedup',
    'table (new entry), the brain lands, and the next seeding round on tab B',
    'can pick it up — even though tab B is training on the triangle track and',
    'never would have discovered it locally.</p>',
    '<p>If tab A\'s user reloads and re-runs the same seed and arrives at',
    '<code>7f3a1c08</code> again, the broadcast still happens — and this time',
    'tab B\'s dedup short-circuits: <code>inserted: false</code>, no new node,',
    'no growth. Same with replays, out-of-order messages, tab C joining late.',
    'The hash is the shared truth.</p>',

    '<p><em>Flag:</em> this is opt-in while the feature bakes. Append',
    '<code>?crosstab=1</code> to the URL on two or more tabs; a small',
    '<code>🔗 N peer(s)</code> pill appears in the training panel and pulses',
    'green on every received brain.</p>',
  ].join('\n'),
};
