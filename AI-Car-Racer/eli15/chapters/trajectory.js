// eli15/chapters/trajectory.js
// ELI15 chapter for SONA trajectories (P2.A).
//
// The reader has already met dynamics-embedding (per-generation video
// recording). This chapter zooms out one level: a *session* is a sequence of
// generations, and a trajectory is the whole session — from "enter phase 4"
// to "leave phase 4". It's the frame SONA's ReasoningBank clusters over.
export default {
  id: 'sona-trajectory',
  title: 'SONA trajectories — framing a whole training session',
  oneLiner: 'A trajectory is the tape recording of one training run — many steps of (what the car saw, how well it did).',
  body: [
    '<p>Until now, each generation has been its own little island: archive the',
    'best brain, score it, move on. But a <em>session</em> — you enter phase 4,',
    'you train for a while, you leave — is actually a <strong>sequence</strong>',
    'of generations on the same track. And sequences carry information that',
    'individual snapshots don\'t.</p>',
    '<p>A <strong>trajectory</strong> is that sequence, bundled up so we can',
    'learn from it as a whole.</p>',
    '<h3 class="eli15-sub">Three moments per session</h3>',
    '<pre class="eli15-ascii">',
    '  phase 4 enter  ──►  beginTrajectory(trackVec)          # open the tape',
    '  gen 1 archived ──►  addStep(dynamicsVec, fitness)      # one step',
    '  gen 2 archived ──►  addStep(dynamicsVec, fitness)      # another step',
    '  gen 3 archived ──►  addStep(dynamicsVec, fitness)      # ...',
    '  ...',
    '  phase 4 leave  ──►  endTrajectory(bestFitness)         # close + learn',
    '</pre>',
    '<p>The three verbs map onto the three moments when SONA\'s state actually',
    'needs to change. Between them, no special magic — we just keep appending',
    'steps to whatever tape is currently open.</p>',
    '<h3 class="eli15-sub">Why not just use one giant fitness number?</h3>',
    '<p>Because "best brain reached fitness 42 at the end" is a <em>single',
    'data point</em>, and single data points are terrible to cluster on. But',
    '"the car started timid, then got faster, then hit a wall pattern around',
    'gen 6, then recovered" is rich — there are intermediate states a learner',
    'can recognise and react to on the next session.</p>',
    '<p>So addStep() captures the intermediate states; endTrajectory() asks',
    'SONA to bake them into patterns before the session tears down.</p>',
    '<h3 class="eli15-sub">What the "activations" actually are</h3>',
    '<p>The plan named them <code>activations</code> because in a big neural',
    'network they\'d be a hidden-layer state. In our little racer we don\'t',
    'have a useful hidden layer to grab, so we reuse two things we already',
    'have:</p>',
    '<ul class="eli15-list">',
    '  <li><strong>dynamics vector</strong> (P1.C, 64 numbers) — how the best',
    '      car drove that generation. Preferred, because it\'s about behaviour.</li>',
    '  <li><strong>track vector</strong> (512 numbers) — a fallback when the',
    '      dynamics recorder missed the generation. About appearance, so it\'s',
    '      less useful, but better than skipping the step.</li>',
    '</ul>',
    '<p>Either way the step arrives at SONA\'s wasm agent as an embedding +',
    'a reward; the agent pads/truncates to its own hidden dim (see',
    '<code>sona/engine.js</code> → <code>coerceSonaVec</code>) so clustering',
    'sees a consistent shape.</p>',
    '<h3 class="eli15-sub">What endTrajectory() actually does</h3>',
    '<p>At the close of the session we replay every step as a',
    '<code>processTask(embedding, normalisedReward)</code> call, tack on one',
    'final call keyed by the <em>trackVec × finalFitness</em>, then call',
    '<code>forceLearn()</code> to run k-means + EWC++ <em>now</em> rather than',
    'on the next 60-second background tick. That last part matters for the',
    'demo — you want to see <code>patterns</code> bump in the panel the',
    'moment you hit Back, not a minute later.</p>',
    '<h3 class="eli15-sub">Try it yourself</h3>',
    '<ul class="eli15-list">',
    '  <li>Enter phase 4. Watch the SONA row in the side panel — the',
    '      <code>traj</code> counter gains a <code>(+open, N steps)</code>',
    '      suffix while your session is live.</li>',
    '  <li>Let it run for ~10 generations, then click Back to leave phase 4.',
    '      <code>patterns</code> should bump from 0 to ≥1.</li>',
    '  <li>Start another session. The "Similar circuits" sub-panel appears,',
    '      showing the clusters the first session minted.</li>',
    '</ul>',
    '<p class="eli15-source">Source: <code>AI-Car-Racer/sona/engine.js</code>',
    '  (<code>beginTrajectory</code> / <code>addStep</code> / <code>endTrajectory</code>);',
    '  <code>AI-Car-Racer/buttonResponse.js</code> wires the phase-4 enter/exit calls.</p>',
  ].join('\n'),
  diagram: [
    '<pre class="eli15-ascii">',
    '  phase 4 enter',
    '       │',
    '       ▼',
    '  [begin]  trackVec ──────────────────────────────┐',
    '       │                                          │',
    '       ▼                                          │',
    '  +----- steps (JS-side buffer) -----+            │',
    '  |  step 1: (dynamics, fitness)     |            │',
    '  |  step 2: (dynamics, fitness)     |  replayed  │',
    '  |  step 3: ...                     |  on end    ▼',
    '  +----------------------------------+  ────► agent.processTask(...)',
    '       │                                                   │',
    '       ▼                                                   ▼',
    '  phase 4 leave ──► [end]  normFitness ──► forceLearn() ──► patterns++',
    '</pre>',
  ].join('\n'),
  related: [
    'dynamics-embedding',
    'reasoningbank',
    'ewc',
  ],
};
