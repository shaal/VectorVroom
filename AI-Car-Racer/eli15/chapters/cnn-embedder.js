// eli15/chapters/cnn-embedder.js
// The CNN that turns a track image into a 512-dim vector.
export default {
  id: 'cnn-embedder',
  title: 'Turning a track picture into 512 numbers',
  oneLiner: 'A tiny CNN squashes a track drawing into a fixed-length vector we can compare.',
  body: [
    '<p>Suppose you\'ve trained on one track, and then you draw a new one. How do you tell',
    'the computer <em>"this new track is kind of like that S-curve one I did last week"</em>?',
    'You need a way to measure track similarity. Our answer is an <strong>embedder</strong>:',
    'a function that turns an image into a fixed-length list of numbers called an',
    '<strong>embedding</strong>.</p>',
    '<p>See <code>ruvectorBridge.js:152</code> — <code>embedTrack(imageData, width, height)</code>',
    'rasterises the finalised track to a 224×224 RGB image, then hands it to a CNN',
    '(<em>convolutional neural network</em>) that outputs a 512-dimensional',
    '<code>Float32Array</code>. The CNN was trained by <em>someone else</em>, ages ago, on',
    'general images; we don\'t retrain it — we just borrow its ability to "describe in',
    '512 numbers what\'s in a picture". The output is L2-normalised (every vector sits on',
    'the surface of a unit sphere) so we can cheaply compare them.</p>',
    '<p><strong>Cosine similarity</strong> is the comparison tool. Given two 512-dim vectors,',
    'the cosine of the angle between them lands in [-1, +1]: 1 means "identical direction"',
    '(visually similar tracks), 0 means "unrelated", -1 means "opposite". Because the',
    'vectors are unit-length, cosine similarity is just a dot product — a single loop',
    'over 512 multiply-adds. Tiny. Fast. See <code>cosineSimilarity()</code> immediately',
    'below <code>embedTrack</code>.</p>',
    '<p>The key insight: the embedder doesn\'t understand racing. It just gives us a reliable',
    'geometric fingerprint. Two tracks with similar shapes get similar vectors, and that\'s',
    'what makes warm-starting possible (see <code>track-similarity</code>).</p>',
    '<h3>Try it yourself</h3>',
    '<ul><li>Advance through phases 1-3 with a new track. Open DevTools; in the console',
    'you\'ll see <code>[ruvector] track embedded — dim=512, head=[...]</code>. Redraw the',
    'same track shape roughly and the <em>first four values</em> of that head will be similar',
    'between runs — that\'s the embedder being shape-stable.</li></ul>',
  ].join('\n'),
  diagram: [
    '<pre class="eli15-ascii">',
    '  track drawing  →  224×224 RGB  →  [ CNN (pre-trained) ]  →  [512 floats]',
    '                                                                   │',
    '                                                              L2-normalised',
    '                                                              (unit sphere)',
    '',
    '  compare two tracks:',
    '     cos(θ) = A · B       (dot product of unit vectors)',
    '            ≈  1   →  very similar shapes',
    '            ≈  0   →  unrelated',
    '            ≈ -1   →  opposite',
    '</pre>',
  ].join('\n'),
  related: [
    'vectordb-hnsw',
    'track-similarity',
    'ema-reranker',
  ],
};
