// eli15/chapters/dynamics-embedding.js
// ELI15 chapter for the P1.C dynamics-trajectory embedding.
//
// The reader has already met the CNN track embedder ("what the track looks
// like" → 512 numbers) and LoRA ("bending that embedding toward usefulness").
// This chapter frames *why* we add a second, different kind of embedding —
// one that describes the car's *behaviour* on the track — and what each of
// the 64 numbers actually measures.
export default {
  id: 'dynamics-embedding',
  title: 'Dynamics embedding — recording how the car drove, not just what it saw',
  oneLiner: 'Turn a whole lap of sensor-and-control readings into a single 64-number vector we can search on.',
  body: [
    '<p>Imagine two photos of a steep hill. They look pretty similar: some grass, some',
    'path, a fence in the corner. Now imagine two <em>videos</em> of running down that',
    'hill — one person sprints confidently, the other slips and stumbles. The photos',
    'told you almost nothing about the runners. The videos told you everything.</p>',
    '<p>That\'s the problem the <strong>dynamics embedding</strong> is trying to solve.</p>',
    '<h3 class="eli15-sub">The problem: two tracks, same photo, different stories</h3>',
    '<p>The CNN track embedder (previous chapter) gives us 512 numbers that describe',
    'what a track <em>looks like</em> from above. That works great for "find me a',
    'similarly-shaped track" — but sometimes two visually similar tracks make our',
    'genetic-algorithm cars behave completely differently. One feels natural and',
    'most seeds converge fast; the other has some tight corner that trips up',
    'everything we\'ve got.</p>',
    '<p>The CNN can\'t tell us that, because it never saw a car drive. It just saw',
    'the picture.</p>',
    '<h3 class="eli15-sub">The fix: embed the video, not the photo</h3>',
    '<p>So every generation, while the best car is driving, we write down a small',
    'row of numbers on <em>every frame</em>:</p>',
    '<pre class="eli15-ascii">',
    'frame 12:  [ray0 ray1 ray2 ray3 ray4  speed  steer  throttle]',
    'frame 13:  [ 0.0  0.1  0.2  0.0  0.0   0.81   +1      +1   ]',
    'frame 14:  [ 0.0  0.2  0.3  0.0  0.0   0.83   +1      +1   ]',
    '            ...',
    'frame 480: [ 0.4  0.7  0.9  0.3  0.1   0.12   -1       0   ]',
    '</pre>',
    '<p>That\'s the <em>trajectory</em> — literally what the car sensed and what it',
    'did, frame by frame. Five sensor rays (how close walls are in five directions),',
    'a signed speed (how fast, forward or reverse), a signed steering signal',
    '(-1 left, 0 straight, +1 right), and a signed throttle (-1 reverse, +1 gas).',
    'Eight numbers per frame, one frame per 1/60 second, for the length of the',
    'best car\'s life.</p>',
    '<p>A full lap might be 600 frames = 4,800 numbers. HNSW can\'t search on that',
    'directly — the vector length has to be <em>fixed</em>, not per-trajectory, or',
    'nearest-neighbour doesn\'t mean anything. So we squash.</p>',
    '<h3 class="eli15-sub">How the squash works: 8 statistics × 8 channels</h3>',
    '<p>For each of the 8 channels, we compute eight summary statistics over the',
    'whole trajectory:</p>',
    '<ul class="eli15-list">',
    '  <li><strong>mean</strong> — on average, was this number big or small?',
    '      (e.g. "did the car spend most of the lap near a wall?")</li>',
    '  <li><strong>std</strong> — how much did it vary? "Is this a wobbly driver?"</li>',
    '  <li><strong>min / max</strong> — extremes. "How close did it ever get to the',
    '      wall? How fast did it go?"</li>',
    '  <li><strong>mean |Δ|</strong> — average frame-to-frame change. "Is it',
    '      jittery, or is it smooth?"</li>',
    '  <li><strong>max |Δ|</strong> — the most sudden change it ever made.',
    '      "Did it ever suddenly slam the brakes?"</li>',
    '  <li><strong>p25 / p75</strong> — the 25th and 75th percentile. Tells us the',
    '      "typical low" and "typical high", robust to single outlier frames.</li>',
    '</ul>',
    '<p>Eight stats × eight channels = 64 numbers. L2-normalise and we\'ve got a',
    'unit vector we can drop into a VectorDB and search exactly like the track',
    'embedding. That\'s the dynamics embedding.</p>',
    '<h3 class="eli15-sub">Why two videos of running down the hill tell you more</h3>',
    '<p>With this extra key in the archive, recommending seeds for a new generation',
    'can now blend two signals:</p>',
    '<pre class="eli15-ascii">',
    '  final_score  =  trackTerm  ×  fitnessTerm  ×  rerankerTerm  ×  dynamicsTerm',
    '                 (photos)     (how good)     (peer pressure)   (videos)',
    '</pre>',
    '<p>Brains whose <em>trajectory</em> matches the current generation\'s live',
    'trajectory bubble up — even if their track looked a bit different — because',
    'they drove in a similar way. This is the P1.C contribution: a second,',
    'behaviour-based retrieval key sitting alongside the appearance-based one.</p>',
    '<h3 class="eli15-sub">Why it\'s off by default</h3>',
    '<p>Two reasons:</p>',
    '<ul class="eli15-list">',
    '  <li><strong>A/B-ability.</strong> The whole project keeps <code>?rv=0</code>',
    '      and per-feature toggles so you can <em>feel</em> what each layer does.',
    '      The dynamics toggle lets you watch the seeding ordering shift as you',
    '      flick it on and off, which is the whole pedagogical point.</li>',
    '  <li><strong>Cold start.</strong> On a brand-new archive the dynamics DB has',
    '      zero trajectories, so the dynamics term contributes nothing useful yet.',
    '      Turning it on after a few generations is when it starts to bite.</li>',
    '</ul>',
    '<h3 class="eli15-sub">Try it yourself</h3>',
    '<ul class="eli15-list">',
    '  <li>Train a few generations with the toggle <em>off</em>. Watch the',
    '      "Seeded from archive" list in the panel — note the top-1 brain id.</li>',
    '  <li>Flick the <strong>dynamics key</strong> checkbox on. The "dynamics key: on ·',
    '      N trajectories" status updates, and on the next <code>begin()</code> the',
    '      seed ordering can shift — brains that <em>drove</em> similarly to the',
    '      current rolling trajectory now get boosted.</li>',
    '  <li>Add <code>?rv=0</code> to the URL. Everything about the bridge — dynamics',
    '      included — short-circuits, so you can compare a GA-only baseline against',
    '      the full vector-memory pipeline.</li>',
    '</ul>',
    '<h3 class="eli15-sub">Why no wasm crate (implementation note)</h3>',
    '<p>The original P1.C plan named the upstream <code>ruvector-temporal-tensor-wasm</code>',
    'crate as the compression engine. We shipped a pure-JS reducer instead — the',
    'upstream crate has only a raw C FFI (no <code>wasm-bindgen</code>), and for a',
    'browser archive of a few hundred brains the JS-side statistical projection',
    'gives equivalent behaviour without the wasm round-trip. See',
    '<code>vendor/ruvector/ruvector_temporal_tensor_wasm/VENDORED.md</code> for',
    'the full reasoning and the follow-up work needed to adopt the wasm crate.</p>',
    '<p class="eli15-source">Source: <code>AI-Car-Racer/dynamicsEmbedder.js</code> — the',
    '   per-frame recorder and 64-dim projection; <code>AI-Car-Racer/ruvectorBridge.js</code>',
    '   — the <code>_dynamicsDB</code> wiring and the <code>dynamicsTerm</code> in',
    '   <code>recommendSeeds()</code>.</p>',
  ].join('\n'),
  diagram: [
    '<pre class="eli15-ascii">',
    '  PHASE-4 GAMEPLAY                     ARCHIVE',
    '',
    '  frame 1:  [r0..r4, v, s, t]   ───┐',
    '  frame 2:  [r0..r4, v, s, t]      │   per-channel stats',
    '  frame 3:  [r0..r4, v, s, t]      ├─► mean, std, min, max,   ─┐',
    '    ...                            │   meanΔ, maxΔ, p25, p75  │  64 numbers',
    '  frame N:  [r0..r4, v, s, t]   ───┘   (8 stats × 8 channels)  │  L2-normed',
    '                                                               │',
    '                                                               ▼',
    '                                                    _dynamicsDB (HNSW)',
    '',
    '  next generation asks: "who drove like I\'m driving now?"',
    '  → search _dynamicsDB with the live rolling vector → blend into score',
    '</pre>',
  ].join('\n'),
  related: [
    'cnn-embedder',
    'vectordb-hnsw',
    'track-similarity',
    'sensors',
  ],
};
