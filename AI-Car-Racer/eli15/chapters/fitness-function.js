// eli15/chapters/fitness-function.js
// How a car is scored — checkpoints + completed laps.
export default {
  id: 'fitness-function',
  title: 'How we decide which car is "best"',
  oneLiner: 'Checkpoints passed + completed laps × track length.',
  body: [
    '<p>The genetic algorithm only works if we have a crisp answer to <em>"which car did best?"</em>',
    'A single number, computed from the trace of the car\'s run. That\'s a <strong>fitness function</strong>.',
    'Ours lives at <code>main.js:167</code> and it\'s deliberately simple:</p>',
    '<pre class="eli15-ascii">',
    'fitness = bestCar.checkPointsCount',
    '        + bestCar.laps × road.checkPointList.length',
    '</pre>',
    '<p>Translation: <em>"how many checkpoints did you pass, plus one lap\'s worth of checkpoints',
    'for every full lap you completed?"</em> Checkpoints are invisible lines stretched across',
    'the road — you place them in phase 2. Every time a car\'s polygon crosses one, its',
    '<code>checkPointsCount</code> ticks up by 1. Once the car has hit every checkpoint in',
    'order, that\'s one lap.</p>',
    '<p>Why not use <em>time</em>? Because the cars are random on generation 0 — most of them',
    'never even move. A "fastest time" metric is meaningless until at least one car can',
    'complete a lap, which might not happen until generation 50. Checkpoint count gives a',
    'partial-credit signal from the very first frame: a car that inched 3 checkpoints forward',
    'is measurably better than a car that crashed immediately.</p>',
    '<p>Note: the best-lap <em>time</em> still gets tracked (see <code>fastLap</code> in',
    '<code>main.js:157-159</code>), but only as a display metric on the "Fast Lap" readout.',
    'The GA itself selects purely on checkpoint-fitness.</p>',
    '<h3>Try it yourself</h3>',
    '<ul><li>In phase 2, drop only 3 checkpoints on a long track instead of the usual 8–12.',
    'Training suddenly stalls: the fitness function has too few gradations to distinguish',
    '"almost finished a lap" from "halfway round". Add more checkpoints and watch progress',
    'resume. The fitness function is the one surface the GA can see.</li></ul>',
  ].join('\n'),
  diagram: [
    '<pre class="eli15-ascii">',
    '                         ━━━━━━━━━━━━',
    '                    ━━━━━            ━━━━',
    '                ━━━━   c1    c2   c3    ━━━',
    '               ━ start ━━━●━━━━━●━━━━━●━━ ━',
    '                  ━      | | | | | |     ━',
    '                   ━     c4        c6    ━',
    '                    ━━━━━ ●━━━━●━━━ ━━━━',
    '                          c5',
    '',
    '   car passes c1, c2, c3 → checkPointsCount = 3',
    '   completes lap (all 6)  → laps = 1',
    '   continues, hits c1, c2 → checkPointsCount = 2',
    '   fitness = 2 + 1×6 = 8',
    '</pre>',
  ].join('\n'),
  related: [
    'genetic-algorithm',
    'neural-network',
    'what-is-this-project',
  ],
};
