// eli15/chapters/track-similarity.js
// The warm-start idea: reuse brains from geometrically similar past tracks.
export default {
  id: 'track-similarity',
  title: 'Not starting from scratch on every new track',
  oneLiner: 'Use brains that did well on similar-shaped past tracks as starting seeds.',
  body: [
    '<p>This is the whole reason the vector-memory bridge exists. By itself, a genetic',
    'algorithm always starts from random noise. Draw a new track → 100% of gen-0 brains',
    'are random → most crash immediately → you watch gibberish for 20 generations before',
    'anything useful emerges. Every. Single. Track.</p>',
    '<p>But generations <em>aren\'t</em> information-free for a human who\'s played before.',
    'You <em>know</em> the new track is basically "like the oval I did last week but with',
    'an S-curve at the end". If a computer could make that kind of judgement, it could',
    'warm-start the new population from brains that did well on the similar old track.',
    'That\'s exactly the pipeline we built:</p>',
    '<ol>',
    '<li><strong>Embed</strong> the new track with the CNN → 512-d vector',
    '(<code>cnn-embedder</code>).</li>',
    '<li><strong>Query</strong> HNSW for the <em>k</em> most similar past track vectors',
    '(<code>vectordb-hnsw</code>). Cosine similarity ∈ [-1, +1].</li>',
    '<li><strong>Retrieve</strong> the brains that were archived alongside each of those',
    'old tracks. These are "brains that drove well on a track that looked like this one".</li>',
    '<li><strong>Rerank</strong> by adding the EMA-learned performance nudge',
    '(<code>ema-reranker</code>).</li>',
    '<li><strong>Seed</strong> the new population with those brains — 1 elite, some light',
    'mutants, some heavy mutants, always a few novelty slots. See',
    '<code>main.js:85-97</code>.</li>',
    '</ol>',
    '<p>The bet: if old track A had a left-hand S-curve and the new track B also has a',
    'left-hand S-curve, the brain that handled A already encodes <em>something</em> useful',
    'about turning left through an S-curve. Mutating it to fine-tune for B is much cheaper',
    'than rediscovering "turn left" from noise.</p>',
    '<p><strong>Honest caveat from measurement</strong>: after the <code>Arch A1\'</code>',
    'change — the network is now <code>8→8→4</code> with two track-orientation inputs (a',
    'scaled-distance vector toward the next checkpoint, projected into the car\'s local frame)',
    '— cross-shape transfer is <em>directional and modest</em>. Training on the harder shape',
    'first (Triangle) then switching to the easier one (Rectangle) shows a positive lift of',
    '~0.05 in last-5 survival (n=3). The reverse direction, Rectangle → Triangle, is roughly',
    'neutral (n=6 combined; Δ ≈ −0.03, down from −0.06 before the arch change). So the',
    'archive no longer <em>actively hurts</em> cross-shape seeding, but it isn\'t a reliable',
    'accelerator in both directions either. See <code>docs/plan/ruvector-proof/phase3.5-v2/',
    'PROOF.md</code> for the full measurement. The behaviour the archive most reliably',
    'delivers is still <strong>save-and-resume on the same track</strong>: warm-restart',
    'faithfully preserves the state the prior session ended at, across every architecture',
    'variant tried.</p>',
    '<h3>Try it yourself</h3>',
    '<ul><li>Finish training on one track, then use the <strong>track preset picker</strong>',
    '(top-left in phase 1) to load a different preset. Advance to phase 4; the Vector',
    'Memory panel will flash an amber <em>"This track is NN% similar to one you\'ve trained',
    'on — loading N candidate brains as seeds"</em> badge. That\'s the warm-start firing.</li></ul>',
  ].join('\n'),
  diagram: [
    '<pre class="eli15-ascii">',
    '   new track image',
    '         │',
    '         ▼  embedTrack()',
    '   [512-d vector Q]',
    '         │',
    '         ▼  VectorDB.search(Q, k=10)',
    '   nearest neighbours: [T1, T2, T3, ...]',
    '         │',
    '         ▼  look up brains archived with each track',
    '   candidate brains: [B1, B2, B3, ...]',
    '         │',
    '         ▼  EMA rerank',
    '   ranked seeds → 1 elite + light + heavy + novelty',
    '         │',
    '         ▼',
    '   gen 0 of the new track starts already half-good',
    '</pre>',
  ].join('\n'),
  related: [
    'cnn-embedder',
    'vectordb-hnsw',
    'ema-reranker',
  ],
};
