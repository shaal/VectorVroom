// eli15/chapters/where-the-time-goes.js
// Phase 3A (F7) — real content. Explains the per-stage timing panel
// mounted below the vector-memory panel in uiPanels.js.
export default {
  id: 'where-the-time-goes',
  title: 'Where each generation\'s milliseconds actually go',
  oneLiner: 'Retrieve, federate, rerank, adapt, dynamics — a flame-graph-lite for every generation, live.',
  body: [
    '<p>Every time the GA asks the archive "who looks like this track?",',
    'a small pipeline of sub-stages runs. Most of them are invisible — the',
    '<code>⏱ Where the time goes</code> panel makes them visible. That',
    'visibility is itself a teaching tool: reading the stacked bar is the',
    'fastest way to build an intuition for what a modern retrieval stack',
    'actually costs, and to spot when something has gone sideways.</p>',

    '<h3>What each stage is doing</h3>',
    '<ul>',
    '<li><strong>retrieve</strong> — the HNSW track search. This is the',
    '    "find nearby tracks" step. When it grows, either the archive got',
    '    bigger (expected, sublinear) or the index rebuilt and we\'re',
    '    paying the cold-start cost (one-off).</li>',
    '<li><strong>federate</strong> — only non-zero when federation is on',
    '    (<code>?federation=1</code> or the toggle). Covers the whole fan-',
    '    out: query both Euclidean AND Hyperbolic HNSW in parallel, union',
    '    by content hash, GNN-rerank the union. Federated runs record',
    '    time here instead of in <em>rerank</em>, because the federation',
    '    body already spans its own rerank pass — double-timing it would',
    '    overcount.</li>',
    '<li><strong>rerank</strong> — the GNN message-passing pass over the',
    '    lineage DAG. Used on the single-index path. It often looks',
    '    bigger than <em>retrieve</em>, because HNSW is O(log N) and the',
    '    GNN is O(candidates × message-passing depth). If you see it',
    '    spiking, try the <em>A/B toggles → reranker → ema</em> option:',
    '    EMA is a much cheaper fallback and the delta between "with GNN"',
    '    and "EMA only" is what the reranker is buying you.</li>',
    '<li><strong>adapt</strong> — the LoRA / SONA track-vector adapter.',
    '    A small matrix-vector product on the query side. Should stay',
    '    under a millisecond for the default topology; if it doesn\'t,',
    '    check the adapter A/B toggle — "off" will bypass it entirely.</li>',
    '<li><strong>dynamics</strong> — the "dynamics key" lookup: compare',
    '    the current generation\'s trajectory embedding against the',
    '    archive to nudge retrieval toward brains that <em>drove</em>',
    '    similarly, not just brains that saw similar tracks. Off by',
    '    default; turning it on adds one extra HNSW query.</li>',
    '<li><strong>misc</strong> — everything else: GA ops, sensor embed,',
    '    dag bookkeeping, persist scheduling. The category exists so the',
    '    stacked bar sums to 100% without hiding work.</li>',
    '</ul>',

    '<h3>How to read the p95 column</h3>',
    '<p>The <em>avg</em> column tells you the typical cost of a stage.',
    'The <em>p95</em> column tells you the <em>tail</em> — the 19th-out-',
    'of-20 worst run in the ring-buffer window. Watching both lets you',
    'spot a stage that is <em>usually</em> cheap but <em>occasionally</em>',
    'expensive, which is the exact pathology that makes a frame-rate look',
    'stuttery even when averages look fine. A healthy pipeline has p95',
    'within ~2-3× of avg. A sudden jump in p95 alone — while avg stays',
    'flat — usually means a rare GC pause or a re-index triggered by an',
    'import. If p95 stays elevated across a window, that\'s a regression',
    'worth a git-bisect.</p>',

    '<h3>The "surprise big number" workflow</h3>',
    '<p>The most productive use of this panel is simple: let the sim run',
    'for a minute, then glance at the stacked bar. Is any one stage',
    '<em>much</em> bigger than you expected? That\'s the stage eating the',
    'frame budget, and it\'s where a quality-vs-latency trade-off will',
    'pay off the most. Concrete examples you can reproduce right now:</p>',
    '<ul>',
    '<li>If <strong>rerank</strong> dwarfs everything else: flip the',
    '    reranker A/B to <code>ema</code>. You trade a few points of',
    '    retrieval quality for a stage that runs in microseconds.</li>',
    '<li>If <strong>federate</strong> dwarfs everything else: turn off',
    '    the federation toggle and compare. Federation doubles the',
    '    HNSW work by design — the teaching moment is that "search two',
    '    geometries" really does cost ~2× "search one".</li>',
    '<li>If <strong>dynamics</strong> is unexpectedly large relative to',
    '    <strong>retrieve</strong>: the dynamics index has more vectors',
    '    than the track index, and that\'s visible as timing. A good',
    '    reminder that what you quantize matters.</li>',
    '</ul>',

    '<h3>Why the window is 20 generations</h3>',
    '<p>Short enough that a real change shows up fast; long enough that a',
    'single GC pause doesn\'t dominate. The window is a ring buffer —',
    'old samples drop off as new ones arrive. The <em>gen</em> label in',
    'the panel header is the latest <code>archiveBrain()</code> call\'s',
    'generation number; it advances as training runs.</p>',

    '<h3>Where this lives in the code</h3>',
    '<p>Timing module: <code>AI-Car-Racer/observability/timings.js</code>',
    '(a ring-buffer histogram per stage name). Panel: ',
    '<code>AI-Car-Racer/observability/panel.js</code> — plain DOM, inline',
    'CSS, ~10Hz rAF poll. Bridge hooks: search <code>ruvectorBridge.js</code>',
    'for <code>_obsStart</code> / <code>_obsTime</code> to see each stage',
    'marked. The smoke harness is <code>tests/observability-smoke.html</code>.</p>',
  ].join('\n'),
  related: [
    'vectordb-hnsw',
    'gnn',
    'federation',
    'lora',
  ],
};
