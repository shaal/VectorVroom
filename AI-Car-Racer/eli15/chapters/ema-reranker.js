// eli15/chapters/ema-reranker.js
// The feedback loop that up- or down-weights retrieved brains by observed fitness.
export default {
  id: 'ema-reranker',
  title: 'Learning which recommendations actually help',
  oneLiner: 'An EMA per retrieved brain nudges future rankings toward ones that paid off.',
  body: [
    '<p>HNSW returns the <strong>visually</strong> most similar past brains for a new track',
    '(same track shape → similar brains did well). But visual similarity isn\'t the same as',
    '<em>actual performance</em>. Sometimes a lower-similarity brain happens to generalise',
    'better. We\'d like the retrieval step to <em>learn from experience</em>.</p>',
    '<p>That\'s what the <strong>EMA reranker</strong> does. See <code>ruvectorBridge.js:162-172</code>:</p>',
    '<pre class="eli15-ascii">',
    'export function observe(retrievedIds, outcomeFitness) {',
    '  const normOut = Math.tanh((outcomeFitness || 0) / 100);',
    '  for (const id of retrievedIds) {',
    '    const prev = _observations.get(id) || { weight: 0, count: 0 };',
    '    const w = EMA_ALPHA * normOut + (1 - EMA_ALPHA) * prev.weight;',
    '    _observations.set(id, { weight: w, count: prev.count + 1 });',
    '  }',
    '}',
    '</pre>',
    '<p><strong>EMA</strong> = Exponential Moving Average. For each retrieved brain id, we keep',
    'a number <em>w</em> ∈ roughly [-1, 1]. After a generation where that brain was a seed,',
    'we update: <em>w_new = α × new_fitness + (1 − α) × w_old</em>. Fresh observations',
    'weigh <em>α</em>; old ones decay at rate <em>(1-α)</em>. The <code>tanh</code> squashes',
    'raw fitness (which can grow arbitrarily large with lap count) down to [-1, 1] so no',
    'single lucky generation dominates.</p>',
    '<p>Next time the same track-vector is queried, HNSW returns candidates ranked by cosine',
    'similarity, and <code>recommendSeeds()</code> then re-sorts them using <em>similarity + w</em>.',
    'Brains that produced bad fitness drift down; brains that paid off drift up. Over',
    'many training sessions, the ordering becomes better than raw visual similarity alone.',
    'Today it\'s a simple scalar nudge — future phases will replace it with a proper graph',
    'neural network.</p>',
    '<h3>Try it yourself</h3>',
    '<ul><li>In the <strong>Vector Memory</strong> panel, watch the row',
    '<code>EMA reranker: N observations · last shift M positions</code>. Every time',
    '<code>observe()</code> fires (end of a generation), M is the number of rank positions',
    'that the top-10 ordering just changed by. Big M = the reranker is still adapting; small',
    'M = the ranking has stabilised.</li></ul>',
  ].join('\n'),
  diagram: [
    '<pre class="eli15-ascii">',
    '   generation k:',
    '     retrieved brains:  [A, B, C, D, E]',
    '     batch fitness:     37  (best car this gen)',
    '                         │',
    '                         ▼',
    '     normOut = tanh(37/100) ≈ 0.354',
    '     for each id:  w ← α·0.354 + (1-α)·w_old',
    '',
    '   generation k+1:  recommendSeeds re-sorts by',
    '                    (cosine_similarity + w)',
    '                    so helpful brains bubble up.',
    '</pre>',
  ].join('\n'),
  related: [
    'vectordb-hnsw',
    'cnn-embedder',
    'lineage',
  ],
};
